# AI 查询功能使用说明

## 功能概述

在微信小程序地图页面中新增了 AI 自然语言查询功能，用户可以通过自然语言查询景点信息，AI 会智能解析查询意图并返回相应的景点数据，然后自动在地图上显示。

## 文件结构

```
myapp/
├── src/
│   ├── components/
│   │   └── AIQueryPanel.vue          # AI 查询面板组件
│   ├── services/
│   │   └── aiQueryService.js         # AI 查询服务封装
│   ├── pages/
│   │   └── map/
│   │       └── index.vue             # 地图页面（已集成 AI 查询）
│   └── utils/
│       └── config.js                 # API 配置（包含 AI 查询端点）
```

## 核心功能

### 1. AI 查询面板 (AIQueryPanel.vue)

**位置**: 固定在地图页面底部中央

**功能特性**:
- ✅ 自然语言输入
- ✅ 会话管理（支持多轮对话）
- ✅ Interrupt/Clarify 处理（当查询不明确时，AI 会要求用户澄清）
- ✅ 查询结果显示（包括答案、数量、查询类型等）
- ✅ 可折叠面板（节省屏幕空间）
- ✅ 智能避让（点击景点时自动折叠，避免与景点详情弹窗重叠）
- ✅ 错误处理

**UI 状态**:
- 正常状态：完整显示查询界面和结果
- 手动折叠：用户点击折叠按钮，只显示输入框和会话指示器
- 自动隐藏：当用户点击景点标记查看详情时，面板自动完全隐藏（淡出并下移），避免与景点详情弹窗的操作按钮重叠

**智能避让机制**:
- 景点弹窗打开时：AI面板自动淡出并向下移动（opacity: 0, translateY: 100rpx）
- 景点弹窗关闭后：AI面板自动恢复显示，淡入并回到原位
- 过渡动画：0.3秒平滑过渡效果

### 2. AI 查询服务 (aiQueryService.js)

**主要方法**:

```javascript
// 生成会话ID
generateSessionId()

// 执行 AI 查询
executeAIQuery(queryText, sessionId)

// 继续执行被中断的查询（clarify 后）
resumeAIQuery(clarifiedQuery, sessionId)

// 获取查询类型的中文名称
getIntentTypeName(intentType)
```

**返回数据结构**:
```javascript
{
  success: true,
  status: 'success', // 或 'interrupt', 'error'
  answer: '查询结果的自然语言描述',
  data: [...],      // 景点数据数组
  count: 10,        // 结果数量
  intent_info: {    // 查询意图信息
    intent_type: 'query',  // 'query' 或 'summary'
    is_spatial: true       // 是否为空间查询
  },
  conversation_id: 'session_xxx' // 会话ID
}
```

## 使用流程

### 基本查询流程

1. **用户输入查询**
   - 在输入框中输入自然语言查询，例如："查询浙江省的5A景区"
   - 点击"🔍 查询"按钮或按回车

2. **AI 处理查询**
   - 系统调用 sight_server 的 `/query` 端点
   - AI 解析查询意图，生成 SQL 语句
   - 执行查询并返回结果

3. **显示结果**
   - 面板显示查询答案和统计信息
   - 地图自动显示查询到的景点
   - 地图自动居中到第一个景点位置

### Interrupt/Clarify 流程

如果查询不明确，AI 会返回 `interrupt` 状态：

1. **显示 Clarify 提示**
   - 面板显示黄色的 clarify 提示框
   - 显示 AI 的疑问和建议

2. **用户输入 Clarify**
   - 在 clarify 输入框中输入更具体的查询
   - 点击"🔄 继续查询"按钮

3. **继续执行查询**
   - 系统调用 `/query/resume` 端点
   - 使用同一个会话ID继续查询
   - 返回最终结果

## 示例查询

### 数据查询类

```
1. "查询浙江省的5A景区"
2. "找出武汉市的所有景区"
3. "显示湖北省评分大于4.5的景区"
4. "查找门票价格低于50元的景区"
```

### 统计汇总类

```
1. "统计湖北省有多少个景区"
2. "统计5A景区的数量"
3. "浙江省有多少个免费景区"
```

### 空间查询类

```
1. "查询武汉市范围内的景区"
2. "找出距离我最近的5A景区"
```

## API 配置

### config.js 配置

```javascript
const SIGHT_SERVER_URL = process.env.NODE_ENV === 'production'
  ? 'https://your-domain.com:8001'
  : 'http://localhost:8001'

const API_CONFIG = {
  sightServerBaseURL: SIGHT_SERVER_URL,

  endpoints: {
    aiQuery: {
      query: '/query',
      session: (sessionId) => `/session/${sessionId}`
    }
  },

  buildURL(endpoint, useSightServer = false) {
    const baseURL = useSightServer ? this.sightServerBaseURL : this.baseURL
    return `${baseURL}${endpoint}`
  }
}
```

### 请求示例

**基本查询**:
```http
GET http://localhost:8001/query?q=查询浙江省的5A景区&include_sql=true&conversation_id=session_xxx
```

**继续查询**:
```http
POST http://localhost:8001/query/resume
Content-Type: application/json

{
  "conversation_id": "session_xxx",
  "clarified_query": "我是指浙江省杭州市的5A景区",
  "include_sql": true
}
```

## 数据处理流程

### 1. 地图页面接收结果

```javascript
handleAIQueryResult(result) {
  // 1. 清空现有景点
  this.clearAllSpots()

  // 2. 转换为地图 markers
  const newMarkers = convertSpotsToMarkers(result.data)

  // 3. 添加到地图
  this.markers = newMarkers

  // 4. 记录已加载的景点ID（防重复）
  result.data.forEach(spot => {
    this.loadedSpotIds.add(spot.id)
  })

  // 5. 地图自动居中到第一个景点
  if (result.data.length > 0) {
    this.center = {
      lng: result.data[0].lng_wgs84,
      lat: result.data[0].lat_wgs84
    }
    this.zoom = 13
  }
}
```

### 2. 景点数据转换

AI 查询返回的景点数据会通过 `convertSpotsToMarkers` 方法转换为地图 markers：

```javascript
{
  id: spot.id,
  latitude: spot.lat_wgs84,
  longitude: spot.lng_wgs84,
  iconPath: getSpotIcon(spot.level),  // 根据等级选择图标
  width: 32,
  height: 32,
  callout: {                           // 点击显示的气泡
    content: spot.name,
    display: 'BYCLICK'
  },
  label: {                             // 始终显示的标签
    content: spot.level,               // 显示等级（5A/4A等）
    bgColor: getLevelColor(spot.level) // 根据等级设置背景色
  },
  spotData: spot                       // 保存原始数据
}
```

## 样式定制

### 面板样式变量

```scss
// 主要颜色
$primary-color: #4a90e2;       // 主色调
$success-color: #4ecdc4;       // 成功色
$warning-color: #ff9800;       // 警告色（interrupt）
$error-color: #ff6b6b;         // 错误色

// 面板尺寸
$panel-width: 680rpx;          // 面板宽度
$panel-bottom: 40rpx;          // 距离底部距离
$panel-max-height: 600rpx;     // 内容最大高度
```

### 自定义样式示例

```scss
// 修改面板位置（移到顶部）
.ai-query-panel {
  bottom: auto;
  top: 100rpx;
}

// 修改按钮颜色
.query-button {
  background: #your-color;
}

// 修改折叠状态高度
.ai-query-panel.collapsed {
  max-height: 200rpx;  // 默认 160rpx
}
```

## 错误处理

### 常见错误及解决方案

1. **无法连接到 AI 查询服务**
   ```
   错误：无法连接到 AI 查询服务，请检查 sight_server 是否启动
   解决：确保后端 sight_server 正在运行（端口 8001）
   ```

2. **查询超时**
   ```
   错误：查询超时，请检查 sight_server 是否正在运行
   解决：检查网络连接，或增加超时时间（默认 60 秒）
   ```

3. **返回数据为空**
   ```
   情况：查询成功但没有找到匹配的景点
   处理：显示"未找到景点"提示，不清空现有地图标记
   ```

## 性能优化

### 1. 会话管理

- 使用会话ID保持多轮对话上下文
- 避免重复创建会话（只在首次查询时创建）
- 会话ID格式：`session_${timestamp}_${random}`

### 2. 数据去重

```javascript
// 记录已加载的景点ID
this.loadedSpotIds = new Set()

// 过滤重复景点
result.data.forEach(spot => {
  if (!this.loadedSpotIds.has(spot.id)) {
    this.loadedSpotIds.add(spot.id)
    // 添加到地图...
  }
})
```

### 3. 请求优化

- 设置合理的超时时间（60 秒）
- 使用 loading 状态防止重复点击
- 错误时显示友好提示

## 开发建议

### 1. 调试技巧

**启用详细日志**:
```javascript
// 在 aiQueryService.js 中
console.log('[DEBUG executeAIQuery] 查询文本:', queryText)
console.log('[DEBUG executeAIQuery] 响应数据:', response)
```

**查看网络请求**:
- 打开微信开发者工具
- 查看 Network 面板
- 检查请求/响应数据

### 2. 扩展功能

**添加历史记录**:
```javascript
data() {
  return {
    queryHistory: []  // 存储查询历史
  }
}

methods: {
  saveToHistory(query, result) {
    this.queryHistory.push({
      query,
      timestamp: new Date(),
      count: result.count
    })
  }
}
```

**添加收藏功能**:
```javascript
methods: {
  favoriteSpot(spot) {
    // 保存到本地存储
    uni.setStorageSync('favorites', [...favorites, spot])
  }
}
```

## 后端要求

### sight_server API 规范

**查询端点**: `GET /query`
- 参数：
  - `q`: 查询文本（必需）
  - `include_sql`: 是否包含 SQL（可选，默认 false）
  - `conversation_id`: 会话ID（可选）

**Resume 端点**: `POST /query/resume`
- Body:
  ```json
  {
    "conversation_id": "session_xxx",
    "clarified_query": "clarify后的查询",
    "include_sql": true
  }
  ```

**返回格式**:
```json
{
  "status": "success",
  "answer": "找到10个浙江省的5A景区",
  "count": 10,
  "data": [...],
  "intent_info": {
    "intent_type": "query",
    "is_spatial": true
  },
  "sql": "SELECT ...",
  "execution_time": 0.123,
  "conversation_id": "session_xxx"
}
```

## 测试清单

- [ ] 基本查询功能
- [ ] Interrupt/Clarify 流程
- [ ] 多轮对话（会话管理）
- [ ] 地图标记显示
- [ ] 地图自动居中
- [ ] 错误处理
- [ ] 加载状态显示
- [ ] 面板折叠/展开
- [ ] 示例查询按钮
- [ ] 响应式布局（不同屏幕尺寸）

## 故障排查

### 问题：查询按钮点击无反应

**检查步骤**:
1. 查看控制台是否有错误信息
2. 确认 sight_server 是否正在运行
3. 检查 config.js 中的 API 地址配置
4. 验证网络连接状态

### 问题：返回结果但地图未显示

**检查步骤**:
1. 查看返回数据中是否包含坐标（lng_wgs84, lat_wgs84）
2. 检查 convertSpotsToMarkers 方法是否正常工作
3. 验证 markers 数组是否正确更新
4. 查看是否有坐标转换问题（WGS84 vs GCJ02）

### 问题：Clarify 流程无法继续

**检查步骤**:
1. 确认会话ID是否正确传递
2. 检查 resume 端点是否可访问
3. 验证 clarified_query 是否正确发送
4. 查看后端日志了解详细错误

## 更新日志

### v1.0.0 (2025-01-21)
- ✅ 实现基本 AI 查询功能
- ✅ 支持会话管理
- ✅ 支持 Interrupt/Clarify 流程
- ✅ 集成到地图页面
- ✅ 自动显示查询结果
- ✅ 可折叠面板设计
- ✅ 完整的错误处理

## 参考资源

- [Uniapp 官方文档](https://uniapp.dcloud.net.cn/)
- [腾讯地图 API](https://lbs.qq.com/miniProgram/jsSdk/jsSdkGuide/jsSdkOverview)
- [LangGraph 文档](https://langchain-ai.github.io/langgraph/)

## 技术支持

如有问题，请查看：
1. 控制台日志（开发者工具）
2. 网络请求详情（Network 面板）
3. 后端 sight_server 日志
4. 本文档的故障排查部分
